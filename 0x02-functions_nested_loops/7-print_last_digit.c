#include "main.h"
/**
 * print_last_digit - last Digit
 *
 * @r: parameter
 *
 * Return: 0;
 *
 */
int print_last_digit(int r)
{
	if (r < 0)
	{
		r = -r % 10;
		return (r);
		return (r);
	}
	else
	{
		r = r % 10;
		return (r);
		return (r);
	}
}
