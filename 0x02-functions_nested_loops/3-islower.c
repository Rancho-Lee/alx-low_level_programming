#include "main.h"
/**
 * _islower - Entry point
 *
 * Description: getting the lower alphabet
 *
 * Return: 0;
 *
 */
int _islower(int c)
{
	if (c _islower)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
