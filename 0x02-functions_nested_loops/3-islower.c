#include "main.h"
/**
 * _islower - Entry point
 *
 * Description: getting the lower alphabet
 *
 * Return: 0;
 *
 */
int _islower(int c)
{
	if (c <= 90)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
