#include <stdio.h>

int main(void)
	/**
	 *main:entry point
	 *
	 * Description:using put instead of printf
	 *
	 * Return:Always 0 (success)
	*/
{
	char Mystr[] = "\"Programming is like building a multilingual puzzle";
	puts(Mystr);
	return (0);
}
