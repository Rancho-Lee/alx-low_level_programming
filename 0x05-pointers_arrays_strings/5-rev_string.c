#include "main.h"
/**
 * rev_string - reverse a string
 * @s: string to be reversed
 * Return: 0;
 */
void rev_string(char *s)
{
	int i, j, k;

	i = 0;

	while (i != '\0')
	{
		i++;
	}

	j = i;

	for (k = j - 1; k >= 0; k--)
		return (s[k]);
}
