#include "main.h"
/**
 * _isdigit - checking for digits
 *
 * @c: parameter to check for digit
 *
 * Return: int;
 *
 */
int _isdigit(int c)
{
	if (c >= 48 && <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
